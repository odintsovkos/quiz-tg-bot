## 1. Помощник перестановки

- [x] 1.1 Добавить в `app/services/quiz/` функцию порядка показа: по числу вариантов и ключу выдачи возвращает список исходных индексов; проверить юнит-тестом, что результат — перестановка всех индексов
- [x] 1.2 Покрыть тестом устойчивость: один и тот же ключ даёт один и тот же порядок, разные ключи дают разные порядки на выборке ключей

## 2. Групповой опрос

- [x] 2.1 В `GroupPublisher.publish` строить переставленный список текстов и пересчитанную позицию верного варианта, передавать их в `send_poll`; проверить тестом с фейковым ботом, что порядок отличается от порядка хранения
- [x] 2.2 Записывать в `GroupPoll.correct_option_id` пересчитанную позицию; проверить тестом, что она указывает на текст верного варианта опубликованного опроса
- [x] 2.3 Проверить тестом приёма ответа, что балл получает участник, выбравший верный вариант на его новой позиции, и не получает выбравший первую позицию, когда там оказался неверный вариант

## 3. Личные режимы

- [x] 3.1 Собирать клавиатуру вопроса сессии (`keyboards.session_question`) по порядку показа с ключом (`session_id`, `position`), оставляя в `callback_data` исходный индекс; проверить тестом соответствие текста кнопки и передаваемого индекса
- [x] 3.2 То же для случайного вопроса (`keyboards.random_question`) с ключом `issue_id`; проверить тестом
- [x] 3.3 Проверить тестом, что буквы вариантов идут по порядку показа сверху вниз
- [x] 3.4 Проверить тестом, что повторная сборка клавиатуры того же вопроса той же сессии даёт тот же порядок

## 4. Сквозная проверка и сдача

- [x] 4.1 Тест на прохождение сессии вопросом с верным вариантом под индексом 0: ответ, засчитанный как верный, и разбор сессии показывают верный вариант правильно
- [x] 4.2 Убедиться, что кабинет администратора показывает варианты в порядке хранения — тестом или проверкой, что перестановка там не применяется
- [x] 4.3 Прогнать `python -m pytest -q`, `python -m mypy app`, `python -m ruff check app tests` — все три чисты
