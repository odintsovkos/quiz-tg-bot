## 1. Каркас проекта и инфраструктура

- [x] 1.1 Создать структуру пакетов `app/{bot,services,repositories,models,core}`, `migrations/`, `data/questions/`, `tests/` и файл `pyproject.toml` с зависимостями (`aiogram>=3`, `SQLAlchemy>=2`, `alembic`, `aiosqlite`, `apscheduler`, `pydantic-settings`, `PyYAML`) и dev-группой (`pytest`, `pytest-asyncio`, `ruff`, `mypy`); проверка — установка окружения проходит, `python -c "import app"` работает
- [x] 1.2 Реализовать `app/core/config.py` на pydantic-settings: `BOT_TOKEN`, `OWNER_ID`, `DB_PATH`, `LOG_LEVEL`, `DEFAULT_TIMEZONE`; проверка — юнит-тест: при отсутствии `BOT_TOKEN` создание настроек падает с ошибкой, называющей недостающий параметр (спека `bot-core`, сценарий «Отсутствует обязательная настройка»)
- [x] 1.3 Реализовать `app/core/db.py`: async-движок SQLAlchemy на `aiosqlite`, фабрика сессий, включение `journal_mode=WAL`, `foreign_keys=ON`, `busy_timeout`; проверка — тест на временной БД подтверждает, что все три PRAGMA применились
- [x] 1.4 Реализовать `app/core/time.py`: перевод момента в действующую таймзону, вычисление даты викторины, проверка попадания времени в окно активности; проверка — юнит-тесты на границе полуночи `Europe/Moscow` и на окно, пересекающее полночь
- [x] 1.5 Настроить структурное логирование и точку входа `app/main.py` с graceful shutdown (остановка планировщика, закрытие движка); проверка — запуск с фиктивным токеном и сигнал остановки завершают процесс без незакрытых ресурсов (спека `bot-core`, сценарий «Остановка процесса»)

## 2. Модель данных и миграции

- [x] 2.1 Описать ORM-модели: `users` (роль, отображаемое имя, username, первый и последний контакт), `settings` (ключ-значение), `chats` (периодичность, окно активности, таймзона, активность); проверка — `alembic revision --autogenerate` создаёт миграцию без ручных правок
- [x] 2.2 Описать модели контента: `questions` (строковый идентификатор как первичный ключ, текст, категория, сложность, активность, пояснение, ссылка, автор и время правки), `question_options` (текст, признак верного); проверка — юнит-тест на каскадное удаление вариантов вместе с вопросом
- [x] 2.3 Описать модели процесса: `quiz_sessions`, `session_questions`, `group_polls` (связь опроса, чата и вопроса), `asked_questions` (чат, вопрос, момент, номер круга); проверка — тест на уникальность пары «опрос и пользователь» и пары «сессия и вопрос»
- [x] 2.4 Описать модели учёта: `answers` (пользователь, вопрос, источник, чат, сессия, верность, момент, дата викторины, флаг учёта), `user_daily_stats`, `user_total_stats`, `user_daily_limits` (запуски викторины, выданные случайные вопросы), `user_topic_preferences`; проверка — тест на уникальность пары «пользователь и дата викторины» в дневных таблицах
- [x] 2.5 Настроить Alembic и создать первую миграцию всей схемы; проверка — `alembic upgrade head` на пустом файле создаёт схему, `alembic downgrade base` откатывает её без ошибок

## 3. Банк вопросов

- [x] 3.1 Реализовать валидатор вопроса в `app/services/content/validation.py`: 2–10 вариантов, ровно один верный, лимиты формата quiz-опроса (вопрос 300, вариант 100, пояснение 200 символов), допустимая сложность; проверка — юнит-тесты на каждое правило по отдельности (спека `quiz-content`, сценарии «Два верных варианта», «Слишком длинный текст для формата выдачи»)
- [x] 3.2 Реализовать чтение файлов-источников JSON и YAML из каталога и сбор всех ошибок партии с указанием файла и идентификатора; проверка — тест на файле с тремя разными нарушениями возвращает все три (спека `quiz-content`, сценарий «Несколько ошибок в партии»)
- [x] 3.3 Реализовать идемпотентный импорт партии в одной транзакции со вставкой-или-обновлением по идентификатору вопроса и отчётом «добавлено / обновлено / без изменений»; проверка — интеграционный тест: повторный импорт тех же файлов даёт нули, ошибка в партии откатывает всё (сценарии «Повторный импорт без изменений», «Два верных варианта»)
- [x] 3.4 Реализовать сохранение вопросов, исчезнувших из файлов, и экспорт банка в формат, пригодный для обратного импорта; проверка — тест кругового цикла «экспорт → импорт в чистую БД → сравнение состава» (сценарии «Вопрос удалён из файла», «Круговой цикл экспорт-импорт»)
- [x] 3.5 Реализовать CLI `python -m app.cli` с командами `import-questions`, `export-questions`, `recalc-stats`; проверка — команды отрабатывают на тестовом каталоге и печатают отчёт
- [x] 3.6 Подготовить `data/questions/README.md` с описанием формата файла и стартовый набор из нескольких вопросов-примеров по 1С; проверка — `import-questions` на этом каталоге проходит без ошибок

## 4. Ядро бота: регистрация, роли, ошибки

- [x] 4.1 Реализовать `UserService`: создание профиля при первом контакте, обновление отображаемого имени при каждом взаимодействии; проверка — тесты на сценарии «Первое обращение в личке», «Первый ответ в группе без предшествующей регистрации», «Смена имени в Telegram»
- [x] 4.2 Реализовать назначение роли владельца по `OWNER_ID` при старте и модель ролей «пользователь / администратор / владелец»; проверка — тест: при первом запуске у идентификатора из конфигурации роль владельца, повторный запуск роль не дублирует
- [x] 4.3 Реализовать middleware регистрации пользователя и фильтры роутеров по роли и типу чата (`private_user`, `private_admin`, `group`, `polls`); проверка — тесты: административная команда и административный callback от участника с ролью «пользователь» отклоняются (спека `bot-core`, сценарии «Обычный пользователь вызывает административную команду», «Устаревшая административная кнопка»)
- [x] 4.4 Реализовать команды `/start` и `/help` с составом по роли и регистрацию списка команд в Telegram; проверка — тесты сравнивают выдачу `/help` для роли «пользователь» и «администратор»
- [x] 4.5 Реализовать глобальный обработчик ошибок и обработку превышения лимита частоты Telegram с повтором после указанной паузы; проверка — тесты: исключение в хендлере даёт нейтральный ответ и запись в лог без трассировки в тексте, ответ с ошибкой лимита приводит к повтору отправки (сценарии «Сбой в обработчике», «Превышение лимита Telegram при рассылке»)
- [x] 4.6 Реализовать модуль текстов `app/bot/texts.py` и типизированные фабрики `CallbackData`; проверка — разбор устаревших данных кнопки возвращает отказ, а не исключение

## 5. Начисление баллов и статистика

- [x] 5.1 Реализовать `ScoringService.record_answer()`: запись ответа с вычисленной датой викторины, определение флага учёта по режиму лимитов и роли, обновление агрегатов в той же транзакции; проверка — юнит-тесты на балл за верный ответ, отсутствие балла за неверный, рост счётчика попыток в обоих случаях (спека `scoring-and-stats`)
- [x] 5.2 Обеспечить идемпотентность записи через уникальные индексы и перехват нарушения уникальности вместо проверки в коде; проверка — тест: две параллельные записи ответа по одной паре «опрос и пользователь» дают одну строку
- [x] 5.3 Реализовать правило флага учёта: в режиме «отключены только для администраторов» личные ответы администраторов и владельца записываются с флагом «не учтён», групповые — учитываются; проверка — тесты на все четыре комбинации режима и источника (спека `scoring-and-stats`, требование «Исключение безлимитных администраторов из личной статистики»)
- [x] 5.4 Реализовать чтение дневной и общей статистики участника с корректной точностью при нуле попыток и место в обоих рейтингах; проверка — тесты на сценарии «Точность при нулевых попытках» и «Новый участник»
- [x] 5.5 Реализовать построение таблицы результатов с сортировкой по баллам, затем по точности, затем по числу попыток, с ограничением числа строк и добавлением собственной строки запросившего; проверка — тесты на сценарии «Запросивший вне верхних строк», «Пустой период», участники без попыток в таблицу не попадают
- [x] 5.6 Реализовать команду пересчёта агрегатов из таблицы ответов (`recalc-stats`); проверка — тест: испорченные агрегаты после пересчёта совпадают с посчитанными по ответам
- [x] 5.7 Реализовать хендлеры личной статистики и таблицы результатов с переключением «за сегодня / за всё время»; проверка — переключение периода пересчитывает таблицу по нужному набору показателей (сценарии «Таблица за сегодня», «Переключение периода»)

## 6. Личные режимы: темы, лимиты, викторина, случайный вопрос

- [x] 6.1 Реализовать `TopicPreferenceService`: сохранение выбора нескольких тем, сброс к «всем темам», вычисление применяемого набора как пересечения с категориями, где есть активные вопросы, с откатом к «всем темам» при пустом пересечении; проверка — тесты на сценарии «Выбор нескольких тем», «Сброс выбора», «Выбранная тема опустела» (спека `private-quiz`)
- [x] 6.2 Реализовать экран выбора тем в личке на inline-кнопках с отметками выбранных и сохранением в БД; проверка — выбор переживает перезапуск процесса (сценарий «Выбор переживает перезапуск»)
- [x] 6.3 Реализовать `LimitService`: атомарная проверка-и-списание лимита одной операцией с условием на текущее значение, отдельные счётчики запусков викторины и случайных вопросов, привязка к дате викторины; проверка — тест на гонку: два одновременных списания при остатке в единицу дают одно разрешение и один отказ
- [x] 6.4 Реализовать режимы лимитов «включены для всех» / «отключены для всех» / «отключены только для администраторов» с чтением из настроек времени выполнения и немедленным применением; проверка — тесты на сценарии «Лимиты отключены для всех», «Лимиты сняты только с администраторов», «Немедленное применение режима», «Возврат лимитов»
- [x] 6.5 Реализовать старт сессии викторины: списание лимита в момент старта, формирование набора вопросов из применяемых тем без повторов, запрет второй параллельной сессии; проверка — тесты на сценарии «Старт сессии», «Повторный старт при активной сессии», «Недостаточно вопросов», «Нет доступных вопросов», «Прерванная сессия не возвращает лимит»
- [x] 6.6 Реализовать прохождение сессии: выдача вопросов по одному с номером и общим числом, приём ровно одного ответа на вопрос, показ верности, верного варианта и пояснения со ссылкой; проверка — тесты на сценарии «Переход к следующему вопросу», «Уникальность вопросов внутри сессии», «Повторное нажатие по отвеченному вопросу»
- [x] 6.7 Реализовать завершение сессии с итогом (верные, всего, баллы, точность, обновлённые дневной и общий результаты), досрочное прерывание и закрытие заброшенной сессии по таймауту; проверка — тесты на сценарии «Сессия пройдена полностью», «Досрочное прерывание», «Заброшенная сессия»
- [x] 6.8 Реализовать одиночный случайный вопрос: выдача сообщением с inline-кнопками, списание собственного лимита, независимость от активной сессии, предпочтение вопросов, на которые участник не отвечал; проверка — тесты на сценарии «Запрос случайного вопроса», «Ответ на случайный вопрос», «Случайный вопрос при активной сессии», «Предпочтение неотвеченных вопросов»
- [x] 6.9 Реализовать сообщения об исчерпании лимита с указанием лимита и времени обновления, показ остатка обоих лимитов и уведомление исключённого участника о том, что его личные ответы не идут в зачёт; проверка — тесты на сценарии «Исчерпан лимит викторин», «Исчерпан лимит случайных вопросов», «Лимиты независимы», «Просмотр остатка», «Итог сессии у исключённого участника»
- [x] 6.10 Реализовать главное меню личных сообщений, связывающее викторину, случайный вопрос, темы, статистику и таблицу результатов; проверка — из меню достижим каждый режим, кнопки от предыдущей версии меню отклоняются без ошибки

## 7. Групповая викторина и планировщик

- [x] 7.1 Реализовать подключение и отключение группового чата с проверкой прав бота на отправку сообщений и опросов; проверка — тесты на сценарии «Успешное подключение», «Нет прав на отправку опросов», «Отключение чата» (спека `group-quiz`)
- [x] 7.2 Реализовать `QuestionSelector` для чата: случайный активный вопрос подходящих категорий вне текущего круга, переход на новый круг с предпочтением давно не задававшихся; проверка — тесты на сценарии «Вопрос не повторяется в пределах круга», «Все вопросы исчерпаны», «Нет доступных вопросов»
- [x] 7.3 Реализовать публикацию вопроса нативным quiz-опросом (неанонимным, с верным вариантом и пояснением) и сохранение связи «опрос — вопрос — чат»; проверка — тесты на сценарии «Публикация вопроса с пояснением», «Связь опроса с вопросом»
- [x] 7.4 Реализовать приём `poll_answer`: определение верности, вызов записи ответа, игнорирование ответа по неизвестному опросу и повторного ответа; проверка — тесты на сценарии «Верный ответ участника», «Неверный ответ участника», «Повторный ответ по тому же опросу», «Ответ на неизвестный опрос»
- [x] 7.5 Реализовать `ScheduleService` на APScheduler с хранилищем задач в SQLite: задача типа `interval` на чат, `coalesce=True`, `misfire_grace_time` в один интервал, проверка окна активности внутри задачи, пересборка задач по таблице чатов при старте; проверка — тесты на сценарии «Публикация по расписанию», «Момент публикации вне окна активности», «Простой бота», «Изменение периодичности на лету»
- [x] 7.6 Реализовать ручной запуск вопроса в чат без сдвига расписания и уведомление администраторов при пустом банке или недоступном чате; проверка — тесты на сценарии «Немедленная публикация», «Нет доступных вопросов», «Недоступность чата» (спека `bot-core`)
- [x] 7.7 Реализовать молчание бота в неподключённых групповых чатах; проверка — тест на сценарий «Бот добавлен в неподключённый чат»

## 8. Кабинет администратора

- [x] 8.1 Реализовать вход в кабинет и корневое меню с разделами по роли (раздел администраторов только владельцу); проверка — тесты на сценарии «Открытие меню администратором», «Раздел администраторов у неполноправного администратора», «Попытка входа обычным участником» (спека `admin-console`)
- [x] 8.2 Реализовать раздел чатов: список с состоянием и расписанием, приостановка и возобновление, отключение, немедленный запуск вопроса; проверка — тесты на сценарии «Просмотр списка чатов», «Приостановка чата», «Ручной запуск из кабинета»
- [x] 8.3 Реализовать раздел расписания: периодичность, окно активности, набор категорий, с валидацией и немедленным применением через `ScheduleService`; проверка — тесты на сценарии «Изменение периодичности», «Некорректное окно активности», «Категории без активных вопросов»
- [x] 8.4 Реализовать раздел вопросов: постраничный просмотр с фильтрами по категории и активности, карточка вопроса, редактирование полей через тот же валидатор, что и импорт, активация и деактивация, фиксация автора и времени правки; проверка — тесты на сценарии «Редактирование вопроса», «Невалидная правка», «Деактивация из кабинета»
- [x] 8.5 Реализовать пошаговое добавление вопроса на FSM aiogram; проверка — тест на сценарий «Добавление вопроса», добавленный вопрос сразу доступен для выдачи
- [x] 8.6 Реализовать раздел лимитов: показ режима и обоих значений, переключение режима с предупреждением о неучёте статистики, изменение значений с отклонением нулевых и отрицательных; проверка — тесты на сценарии «Просмотр текущих лимитов», «Переключение режима», «Предупреждение о неучёте статистики», «Изменение значения лимита», «Недопустимое значение»
- [x] 8.7 Реализовать раздел общих настроек: размер сессии, число строк рейтинга, таймзона суточного сброса, с применением без перезапуска; проверка — тесты на сценарии «Изменение размера сессии», «Изменение таймзоны» (сохранённые дневные итоги не пересчитываются)
- [x] 8.8 Реализовать раздел сводной статистики (участники, активные за сегодня, заданные вопросы за сегодня и за всё время, доля верных ответов, размер банка с разбивкой); проверка — тест на сценарий «Просмотр сводки»
- [x] 8.9 Реализовать управление администраторами для владельца: список, назначение, снятие, запрет снять самого себя, немедленное действие снятия на открытые меню; проверка — тесты на сценарии «Назначение администратора», «Снятие прав», «Попытка снять владельца»

## 9. Проверка целостности поведения

- [x] 9.1 Написать интеграционный тест суточной границы: ответы до и после полуночи `Europe/Moscow` попадают в разные дни, вчерашние итоги сохраняются, общая статистика не обнуляется; проверка — тест проходит с подменой времени (спека `scoring-and-stats`, сценарии «Наступление полуночи», «Бот не работал в полночь», «Просмотр итогов прошедшего дня»)
- [x] 9.2 Написать интеграционный тест сквозного пути «групповой опрос → пять верных ответов → таблица результатов»: каждый отвечавший получил по баллу и виден в дневном рейтинге; проверка — тест проходит (сценарий «Несколько верных ответов в группе»)
- [x] 9.3 Написать интеграционный тест режима «отключены только для администраторов»: личные ответы администратора не меняют агрегаты, групповые меняют, обычный участник по-прежнему упирается в лимит, смена режима не пересчитывает прошлое; проверка — тест проходит
- [x] 9.4 Настроить `ruff` и `mypy` в строгом режиме на пакет `app`; проверка — обе проверки проходят без ошибок
- [x] 9.5 Настроить прогон всего набора тестов одной командой и убедиться, что тесты не зависят от порядка и от реального Telegram API; проверка — `pytest` проходит дважды подряд на чистом окружении

## 10. Развёртывание и документация

- [x] 10.1 Подготовить `Dockerfile` и `docker-compose.yml` с томом под файл SQLite и переменными окружения; проверка — контейнер собирается и стартует с тестовым токеном, файл БД сохраняется между перезапусками контейнера
- [x] 10.2 Подготовить `.env.example` со всеми переменными развёртывания и пояснениями; проверка — запуск по инструкции из чистого клона доходит до подключения к Telegram
- [x] 10.3 Написать `README.md`: установка, миграции, импорт вопросов, запуск, подключение группового чата, настройка расписания и лимитов, резервное копирование файла БД; проверка — коллега проходит путь развёртывания по документу без обращений к коду
