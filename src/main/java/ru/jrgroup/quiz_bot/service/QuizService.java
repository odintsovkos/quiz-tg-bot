package ru.jrgroup.quiz_bot.service;

/**
 * Сервис для бизнес-логики викторин.
 *
 * - startQuizForUser(Long userId) — начало викторины для пользователя
 * - getNextQuestion(Long userId) — следующий вопрос пользователю
 * - checkAnswer(Long userId, Long questionId, int answer) — проверка ответа
 * - finishQuiz(Long userId) — завершение викторины, подсчёт результатов
 */

public class QuizService {
}
