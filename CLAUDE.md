# quiz-tg-bot

Telegram-бот с викторинами. Python + aiogram 3 + SQLite. Предметная область
задаётся банком вопросов, в коде её нет.

## Код бота

Бот написан и работает. Проверки перед сдачей работы: `python -m pytest -q`, `python -m mypy app`,
`python -m ruff check app tests`. Все три должны быть чистыми.

Слои: `handlers → services → repositories → models`. Обработчик не ходит в БД
мимо репозитория и не считает бизнес-правила сам.

| Каталог | Что внутри |
|---|---|
| `app/bot/` | Роутеры по зонам, хендлеры, клавиатуры, тексты, middleware |
| `app/services/` | Правила: сессии, лимиты, темы, расписание, статистика, импорт |
| `app/repositories/` | Доступ к данным |
| `app/models/` | ORM-модели; `migrations/` — alembic |
| `app/core/` | Конфигурация, движок БД, планировщик, логирование, время |

### Ключевые решения, чтобы не переспрашивать

- Групповая викторина — нативный quiz-опрос Telegram, балл всем ответившим верно.
- Моменты автопубликации отсчитываются от начала окна активности
  (`publication_slots`), и на каждый — своя `cron`-задача. Сетка зависит только
  от настроек чата: ни время запуска процесса, ни правка настроек её не сдвигают.
  Вне окна активности моментов не существует, проверять окно при срабатывании
  незачем.
- Дневная статистика обнуляется в полночь `Europe/Moscow`.
- Кабинет администратора живёт внутри бота, лимиты в личке настраиваются оттуда.
- **В личке единственный экран-якорь.** Переход между режимами — правка одного
  сообщения, а не новое. Весь личный вывод идёт через `app/bot/replies.py`;
  добавлять `message.answer` в обход нельзя. Исключение — лента сессии
  викторины: её сообщения учитываются в реестре и убираются при завершении.
- Команды участника в личке удаляются после обработки.
- Групповые чаты модели якоря не подчиняются: опрос правкой не заменить,
  а чужие сообщения бот удалять не вправе.

### Грабли, на которые уже наступали

- `DatabaseMiddleware` и `UserMiddleware` регистрируются как **outer**: inner
  отрабатывает после фильтров, и `IsAdmin`/`ConnectedChat` молча отвергают всё.
- Внутри обработчика нельзя открывать вторую сессию БД: транзакция события
  держит блокировку SQLite до возврата, и вложенная запись её не дождётся.
  Сервисы, которые зовут и из обработчика, и из планировщика, принимают
  `session` необязательным параметром.
- Задачи APScheduler живут в памяти и пересобираются при старте по таблице
  чатов. Хранилище задач в той же БД давало второго писателя и блокировки.
- У `IntervalTrigger` без `start_date` APScheduler подставляет `now + interval`.
  Сетка привязывалась к моменту `add_job`, поэтому бот, поднятый в 19:22,
  публиковал первый вопрос в 10:22, а не в начале окна. Отсюда `cron`-задачи
  по временам суток.
- `TelegramNetworkError` и `TelegramServerError` — подклассы
  `TelegramAPIError`. Один общий `except` делал обрыв связи неотличимым от
  исключения бота из чата, и чат отключался от каждого сетевого сбоя.
  Временные отказы перечислены в `DEFERRING_ERRORS` и ловятся раньше общего.
- Клавиатуры со списком из банка (темы, категории чата) обязаны листаться
  страницами: под сотню кнопок Telegram отвергает как слишком длинную разметку.
- Тесты, вызывающие обработчики напрямую, не проверяют ни фильтры, ни порядок
  middleware. Для этого есть `tests/test_dispatch.py` — событие идёт через
  настоящий диспетчер.
- `batch_alter_table` в alembic пересоздаёт таблицу: копия, `DROP TABLE`,
  переименование. Миграции идут через тот же `create_engine`, что и бот, а он
  держит `PRAGMA foreign_keys=ON` — и этот `DROP` уносит каскадом всё, что
  ссылается на строку. Правка `questions` так стёрла варианты ответа и всю
  историю ответов на рабочей базе. Пересоздание таблицы оборачивается снятием
  проверки ключей вне транзакции (`PRAGMA` внутри транзакции не действует),
  образец — в миграции `f1b07c4a92de`. Добавление столбца безопасно: его
  SQLite умеет без пересоздания. Проверяет `tests/test_migrations.py` —
  миграция гоняется на непустой базе.

## Банк вопросов

Сами вопросы в репозиторий не входят: они собраны по локальной копии
документации платформы, а она вместе со скриптами сборки остаётся на машине
автора (см. `.gitignore`). В репозитории — только формат
(`data/questions/README.md`) и образец раскладки (`data/questions/example/`).

Что важно знать при работе с кодом:

- Один файл — одна глава одного руководства, она же одна тема в боте.
  `id` вида `dev.ch08.001` стабилен при правках — по нему живёт статистика.
- Лимиты нативного quiz-опроса Telegram: вопрос 300 символов, вариант 100,
  пояснение 200. Их проверяет импорт и правка вопроса в кабинете.
- Ровно 4 варианта ответа, один верный.
- Импорт идемпотентен и ничего не удаляет: вопрос, исчезнувший из файлов,
  остаётся в банке вместе с накопленной статистикой.
